<!DOCTYPE html>
<html>
	<head>
		
			<script type="text/javascript" src="http://use.typekit.com/slk0fzn.js"></script>
			<script type="text/javascript">try{Typekit.load();}catch(e){}</script>
			
		<title><%= title %></title>
		<!-- It prevents cross-site request forgery (CSRF). -->
		<%= csrf_meta_tag %>

		<!-- It replaces the stylesheets lines. The effect of this line is to look for a file called app/views/layouts/_stylesheets.html.erb, evaluate its contents, and insert the results into the view -->
		<%= render 'layouts/stylesheets' %>
		<!-- Web browser's can't send DELETE requests natively, so Rails fakes them with JavaScript. To get the delete links to work, it has to include 
			the default Rails JavaScript libraries, by adding the next line -->
		<%= javascript_include_tag :defaults %>
	</head>

	<body>
		
		<div class="container">
			<%= render'layouts/header' %>
			<section class="round">
				<% flash.each do |key, value| %>
				<!--	<div class="flash <%= key %>"><%= value %></div> -->
				
					<%= content_tag(:div, value, :class => "flash #{key}") %>
				<% end %>
				<!-- For example this layout ensures that visiting the page /pages/home converts the contents of home.html.erb to HTML and then inserts it in place of <% yield %> -->
				<%= yield %>
			</section>
			<%= render 'layouts/footer' %>
			<%= debug(params) if Rails.env.development? %>
		</div>
	</body>
</html>
