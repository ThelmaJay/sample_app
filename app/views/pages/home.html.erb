<% if signed_in? %>
	<%= render 'shared/signed_in_home'%>
	
	<!-- Adding follower stats to the Home page -->
	<%= render 'shared/user_info'%>
	<%= render 'shared/stats'%>
<% else %>
	<%= render 'shared/signed_out_home'%>
<% end %>
